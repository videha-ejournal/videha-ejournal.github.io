name: Build Root Videha General Search

on:
  push:
    branches: [main]
    paths-ignore:
      - 'pagefind-videha-search/**'
      - 'pagefind-quiz/**'
      - 'data/**'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: videha-root-generated-search-main
  cancel-in-progress: false

jobs:
  build-search:
    runs-on: ubuntu-latest
    steps:
      - name: Sparse checkout HTML corpus and tools
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          sparse-checkout-cone-mode: false
          sparse-checkout: |
            /*.html
            /*.htm
            /**/*.html
            /**/*.htm
            /.github/
            /tools/
            /pagefind-videha-search/

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Prepare non-quiz root corpus
        run: python3 tools/videha-root-general-pagefind-prepare.py . _pagefind_videha_build

      - name: Build Pagefind
        run: npx -y pagefind@1.5.2 --site _pagefind_videha_build --output-subdir pagefind-videha-search --glob '**/*.{html,htm}'

      - name: Install search bundle
        run: |
          rm -rf pagefind-videha-search
          cp -R _pagefind_videha_build/pagefind-videha-search ./pagefind-videha-search
          rm -rf _pagefind_videha_build

      - name: Commit refreshed root general index safely
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pagefind-videha-search
          if git diff --cached --quiet; then
            echo "Root general search unchanged."
            exit 0
          fi
          git commit -m "Build root Videha general search index"
          for attempt in 1 2 3; do
            git fetch origin main
            git rebase origin/main
            if git push origin HEAD:main; then
              exit 0
            fi
            echo "Push raced with another writer; retrying ($attempt/3)..."
            sleep $((attempt * 2))
          done
          echo "Unable to push generated root general index after 3 attempts."
          exit 1
